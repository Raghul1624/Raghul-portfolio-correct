import React,{useState} from 'react';import Reveal from './Reveal';import './Contact.css';
function Contact(){const[form,setForm]=useState({name:'',email:'',message:''});const[errors,setErrors]=useState({});const[submitted,setSubmitted]=useState(false);const handleChange=e=>setForm(p=>({...p,[e.target.name]:e.target.value}));const validate=()=>{const e={};if(!form.name.trim())e.name='Name is required.';if(!form.email.trim())e.email='Email is required.';else if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(form.email))e.email='Enter a valid email address.';if(!form.message.trim()||form.message.trim().length<10)e.message='Message should be at least 10 characters.';return e};const handleSubmit=e=>{e.preventDefault();const v=validate();setErrors(v);if(!Object.keys(v).length){setSubmitted(true);setForm({name:'',email:'',message:''});setTimeout(()=>setSubmitted(false),5000)}};return <section id="contact" className="section contact"><div className="container"><div className="contact-grid"><Reveal className="contact-info"><p className="eyebrow">/ Let's Connect</p><h2 className="section-title contact-title">Let's create something <em>useful.</em></h2><p className="section-sub contact-sub">Open to entry-level Java Developer roles and opportunities to learn, contribute and grow.</p><div className="contact-details"><a href="mailto:raghulvishnu533@gmail.com" className="contact-detail"><span className="contact-label">Email</span><span>raghulvishnu533@gmail.com</span></a><a href="tel:+916384076150" className="contact-detail"><span className="contact-label">Phone</span><span>+91 6384076150</span></a><div className="contact-detail"><span className="contact-label">Location</span><span>Chennai, Tamil Nadu</span></div></div></Reveal><Reveal className="card contact-form-card" delay={120}><form className="contact-form" onSubmit={handleSubmit} noValidate><div className="form-group"><label htmlFor="name">Name</label><input id="name" name="name" value={form.name} onChange={handleChange} placeholder="Your name"/>{errors.name&&<span className="form-error">{errors.name}</span>}</div><div className="form-group"><label htmlFor="email">Email</label><input id="email" name="email" type="email" value={form.email} onChange={handleChange} placeholder="you@example.com"/>{errors.email&&<span className="form-error">{errors.email}</span>}</div><div className="form-group"><label htmlFor="message">Message</label><textarea id="message" name="message" rows="5" value={form.message} onChange={handleChange} placeholder="Tell me about the opportunity..."></textarea>{errors.message&&<span className="form-error">{errors.message}</span>}</div><button className="btn btn-primary contact-submit" type="submit">Send Message ↗</button>{submitted&&<p className="form-success">Thanks for reaching out — I'll get back to you soon.</p>}</form></Reveal></div></div></section>};export default Contact;
